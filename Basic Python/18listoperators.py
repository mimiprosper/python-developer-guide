# list of colors
colors = ['red', 'blue', 'green', 'brown', 'purple']
print(colors)

# add color peach to list of colors
colors.append("peach")
print(colors)

# add another list to the list of colors
colors = colors + ['black', 'white']
print(colors)

